# CUDA 优化实施进度

## 2026-09-05 当前 HEAD 验证与分步提交

- Full Access 已确认 RTX 5080 Laptop GPU 可用：driver 610.57.04、CUDA 13.3、compute capability 12.0。
- 当前代码候选已拆分为两个本地提交：`fbe7685`（engine completion/trusted launch）和 `ccd306a`（mutual/state/solver/test 修复）；未推送远端。
- CUDA Release 构建成功；`cuda-quick` 30/30、`cuda-parallel` 30/30、`cuda-slow` 7/7、`cuda-integration` 8/8 通过。
- `test_gpu_solver`、`test_gpu_resident_pipeline`、`test_gpu_graph_pipeline` compute-sanitizer memcheck 均为 0 errors；最终 benchmark 六份 artifact 均通过 schema checker，每份 165 GPU rows。
- 初次 benchmark 因遗漏 `COILGUN_BENCHMARK_COMMIT` 生成 `unrecorded` metadata，未作为证据；随后按正确 SHA 重跑并记录最终 artifact hash。
- 当前剩余：最终 review、文档提交、stale worktree/临时 artifact 精确清理，以及提交后最终验证。

## 2026-08-19 R25 completion 优化恢复

- 重新读取设计文档和执行计划，确认 B2-T3 的 completion 要求为收集 completion mask、合并清零和必要同步；最终 benchmark 仍严格延后。
- 当前工作树的 R25 候选已将 `complete_stage()` 从“每次 `cudaMemsetAsync` 后立即同步”改为 host 收集待清零位置，在下一次 physical pipeline 的 execution stream 上统一入队清零，并由既有 pipeline barrier 完成一次必要同步。
- `test_gpu_resident_pipeline` 的新增 completion 回归已在 CUDA Release 构建并通过 `ctest`（1/1）；当前仍需完成 API 语义同步、异常/重复 completion 生命周期审查和 R25 正式 clean review。
- 设计中的“一次清零”按稀疏 completion 解释为一个延迟清零阶段：暂不引入额外 device index buffer、H2D 和独立 clear kernel；对少量 completion，这些额外 launch/transfer 成本可能超过多个同一 stream 的异步 memset。该取舍需在最终 benchmark 中以 B/S workload 证据复核，不能在 benchmark 前宣称收益。

- B4-T1 恢复执行：worktree `.worktrees/b4-t1` 的定向 CUDA Release 构建 49/49 成功；定向运行 `test_gpu_execution_policy` 29/29、`test_gpu_paths` 11/11、`test_gpu_graph_pipeline` 9/9、`test_gpu_resident_pipeline` 2/2、`test_gpu_solver` 20/20 全部通过（130、182、49、4、24783 assertions）。
- B4-T1 首次 `ctest --preset cuda-integration` 结果为 0/8：worktree 仅构建了定向目标，8 个 integration 可执行文件不存在，均为 Not Run；已记录为构建前置缺口，下一步补齐完整 CUDA Release build 后重跑，不视为代码行为失败。
- B4-T1 补齐 CUDA Release 全量构建 101/101 成功；首次完整 integration 为 7/8，`test_gpu_sim_batch` explicit backend equivalence 出现既有 force 差异；第二次完整 integration 仍 7/8，但失败点变为既有 E3 active-mask continuation 的 `muzzle_velocity` 差异。相同 worktree 的 `test_gpu_sim_batch` 单独运行连续 5 次通过，root `f1c128e` 的基线二进制也复现前一类差异，因此判定为既有跨测试/运行时数值波动，未修改 tolerance 或越界文件。
- B4-T1 已通过 Luna/High/No Fast review（Spec compliant、Task quality Approved、0 Critical/Important、1 Minor），以 `420064f` 集成；集成分支 CUDA Release 增量构建成功，CUDA quick 30/30 通过。已清理 `.worktrees/b4-t1` 和 `task/cuda-opt-b4-t1`。
- B4-T2 已完成并集成。接手时 WIP 已在 `.worktrees/b4-t2` 落盘（9 文件、未提交）；controller 核验 brief 写集无越界后，补齐 CUDA Release 全量构建 110/110、`test_gpu_graph` 10/10、`test_gpu_graph_pipeline` 11/11、`cuda-quick` 30/30。`cuda-integration` 首轮 6/8，`test_gpu_vs_cpu_multi` 与 `test_gpu_sim_batch` 失败，两者隔离单跑分别 36/36、17/17 全过，判定为既有跨测试/运行时数值波动（B4-T1 已记录同源现象），本 Task 数值中性，波动延后 B5。唯一 Task commit `ea92436`，controller fixed-package review clean（0 findings），集成 commit `cff073c`。已清理 worktree 与分支。
- B4 Gate 通过（开发正确性与文档同步）：Graph key 仅含不可变拓扑/布局/策略，runtime mask 为 device input；`BackendSelectionReason` 经 `ExecutionReport` 暴露（to_string/merge/冲突检测），API.md/API_cn.md 与 benchmark schema 双语同步；正式性能门禁延后 B5。
- 进入 B5：B5-T1 全量验证与最终效果评估。

## 2026-07-28

- 读取并核对设计文档、执行计划和项目 AGENTS.md。
- 读取 planning/worktree/SDD/TDD 技能要求。
- 完成 git/worktree/ledger/subagent 能力预检。
- 提交 `89e9f36 chore: ignore task workspaces`，仅增加 SDD/worktree 忽略规则。
- 开始创建 B0-T1 worktree 和基线验证。
- 创建 `.worktrees/b0-t1` / `task/cuda-opt-b0-t1`，base `89e9f36`。
- CPU Release CTest 21/21 通过；CUDA Release CTest 37/37 通过。
- 准备阶段完成，进入 B0-T1。
- B0-T1 implementer 完成 `35b3b16`；build 通过、CUDA quick 30/30、三轮各 165 行 schema 校验通过。
- 首次直接执行 review-package 因脚本无 executable bit 失败，改用 bash 调用。
- B0-T1 task review 返回 Needs fixes：2 Important、1 Minor；停止集成并派回同一 implementer amend。
- B0-T1 implementer 将 2 Important、1 Minor 全部修复并 amend 为 `f0f10a7`。
- B0-T1 二次 review：Spec Compliant、Task Quality Approved，无 Critical/Important/Minor。
- Controller 复核单提交、干净 worktree、CUDA quick 30/30、三份 165 行最终 artifact 全部 schema PASS。
- 关闭 B0-T1 implementer/reviewer；后续 subagent 固定 `gpt-5.6-sol`、High、No Fast。
- 将 B0-T1 cherry-pick 为集成提交 `9e50677`；下一 Task 为 B0-T2 固定 workload、环境和数值基线。
- 删除已集成的 `.worktrees/b0-t1` 和 `task/cuda-opt-b0-t1`，保留三份最终 raw artifact。
- 从 `9e50677bf04b...` 创建 `.worktrees/b0-t2` / `task/cuda-opt-b0-t2`，并填写 B0-T2 brief。
- B0-T2 隔离基线通过：CPU Release 21/21、CUDA quick 30/30、CUDA integration 8/8。
- CUDA build 通过；仅观察到既有 `BOOST_MATH_ENABLE_CUDA` 重定义、Eigen constexpr 和 volatile increment warnings。
- B0-T2 首个 Sol High implementer 线程持续 running 但无响应、无文件/进程进展；普通状态请求和 interrupt 后仍停滞，已关闭。
- 保持 `gpt-5.6-sol` / High / No Fast 原配置重新派发 B0-T2，不做模型降级或模式切换。
- 第二个 Sol High implementer 同样在 wait/interrupt 后无响应、无落盘或进程活动，已关闭。
- 触发计划的平台阻塞回退：controller 本地执行 B0-T2；不使用其他 subagent 模型或 Fast。
- B0-T2 本地完成当前-base 三轮 benchmark、统计、数值快照和中文 baseline 文档；唯一 commit `b04002b`。
- `review-package` 默认路径解析因内部 `sdd-workspace` 无 executable bit 失败；改为显式 OUTFILE 调用。
- Sol High reviewer 同样无响应，已关闭；controller 本地 review 发现并补齐 precision、Simulations/s 完整统计和 multi-stage 实际数值快照。
- B0-T2 amend 后唯一 final commit 为 `ddc8ea0`，等待重新生成 package 和最终本地 verdict。
- B0-T2 final package `9e50677..ddc8ea0` 本地 task review clean：Spec Compliant、Task Quality Approved，0 findings。
- 将 B0-T2 cherry-pick 为集成提交 `886a859`。
- B0 Gate 通过：两 Task 唯一提交/review clean；CPU/CUDA 基线通过；schema、固定 workload、环境、fallback 和数值快照完整。
- B0 完成，进入 B1-T1；先清理 B0 worktree、分支、raw stdout、临时工具和 Task handoff 文件。
- B1-T1 CUDA Release 定向构建完成；solver/resident/assembly/resource-contract 基线 4/4 通过。
- B1-T1 TDD RED：新增奇异 device system 用例；旧实现按预期在 `REQUIRE(enqueue_status.ok)` 失败，证明 `solve_device()` 提前同步并消费状态。
- B1-T1 GREEN：`solve_device()` 改为 enqueue-only，Engine batched path 由 validation 承担唯一 barrier，Eigen fallback 保留同步；singular focused 1/1（8 assertions）和回归 4/4 通过。
- B1-T1 首次 `cuda-integration` 因定向构建缺少 8 个测试可执行文件而 0/8 Not Run；转为先构建完整 CUDA Release，不计为测试行为失败。
- B1-T1 完整 CUDA Release 构建通过，随后 `cuda-integration` 8/8 通过。
- B1-T1 focused `compute-sanitizer --tool memcheck` 通过，0 errors。
- B1-T1 solver microbenchmark base/after 各 3 轮完成；12 个 D/B shape status/residual/solution 全通过，D=9 total median 改善约 21%，D=32 约 7%，D=129 约 0.5%--0.8%。
- B1-T1 B0 fixed workload base/after 各 5 轮完成，10 份 artifact 均含 204 行 benchmark 输出（落盘含终止空行，`wc -l=205`）、165 GPU rows 且 schema strict check 通过；确认设备双模噪声，端到端未形成 5% 稳定收益，不改变默认 solver policy。
- B1-T1 候选唯一 Task commit `f0cebf5`；Task report 与 `886a859..f0cebf5` review package 已生成，派发 `gpt-5.6-sol` High/No Fast 独立 reviewer `Wegener`。
- B1-T1 Sol High reviewer 在两个 wait 和一次 interrupt 后仍无返回并保持 running，已关闭；未切换模型/Fast。
- B1-T1 controller 按固定 package 完成本地 spec/quality + Brooks review：Spec compliant、Task quality Approved、Health 100/100，0 Critical/Important/Minor。
- B1-T1 已 cherry-pick 到集成分支，集成提交 `920778c`。
- 恢复时原定向构建 session 已关闭且无法回取退出码；在集成分支重跑相同增量构建，exit 0，随后 focused CUDA tests 4/4 通过。
- B1-T1 完成并进入清理；下一 Task 为 B1-T2 wrapper solver policy 与重复 CPU gradient 消除。
- B1-T1 干净 worktree 与临时分支已删除；15 个 `/tmp` 产物逐项 `unlink`，brief/report/review 保留到 B1 Gate。
- 从 `920778c` 创建 `.worktrees/b1-t2` / `task/cuda-opt-b1-t2`。
- B1-T2 baseline：CUDA Release 定向构建 exit 0；single/multi/path 三个指定测试直接运行 3/3 通过（`cuda-quick` 标签只选中 path，已避免误报为完整 focused suite）。
- 确认 wrapper 的 Auto solver 转发已由既有 `3ddd86e` 提供；B1-T2 剩余生产缺口是 single-stage termination 重复 CPU mutual gradient。
- 创建 `.superpowers/sdd/task-B1-T2-brief.md`，限定 wrapper/header/tests 写集并要求 TDD、交错 benchmark 和 policy/report 证据。
- 派发 B1-T2 implementer `Dalton`（agent `019fa785-0012-7160-9936-21f8af041ebf`），显式 `gpt-5.6-sol` / High / No Fast；其运行期间由该 agent 独占 GPU。
- B1-T2 implementer 两个 wait 窗口无活动；interrupt 后返回 BLOCKED。关闭 agent 后发现仅有一个未提交测试草稿，生产代码未改；controller 依平台回退条款继续。
- B1-T2 TDD valid RED：committed force 阈值用例在旧实现上继续到 step 3，预期 step 2 的 2 个断言失败；GREEN 后 focused 1/1、5/5 assertions 通过。
- 删除 single wrapper CPU mutual-gradient helper；velocity termination 复用最后 committed engine force。补充 small/large Auto solver 与 reset metadata 测试。
- B1-T2 focused wrapper/path 3/3、完整 CUDA Release build、cuda-integration 8/8 通过。
- B1-T2 交错 benchmark：Direct velocity median 改善 78.639%，Graph velocity 72.799%，Direct control 0.008%；其余 workload 无超过 2% 回退，最大数值差 `7.4824e-9`。
- 创建唯一 Task commit `22f848c` 与 Task report；生成固定 review package，派发 reviewer `Maxwell`（`gpt-5.6-sol` / High / No Fast）。
- B1-T2 初审发现 1 Important：empty history 在 `velocity_decay_steps=0` 时被当作 committed zero force；按 TDD 修复并 amend 为 `d9106a8`。
- 修复后 focused 3/3、cuda-integration 8/8 通过；re-review 首次仅剩 report-only Minor，修正文案后最终 Spec Approved、Task Quality Approved、0 findings。
- 将 B1-T2 cherry-pick 为集成提交 `3d254b6`；集成分支定向构建、focused 3/3、cuda-integration 8/8、cuda-quick 30/30 通过。
- B1 Gate 通过：两 Task 单提交/review clean；D/B solver 数值矩阵、Eigen/device/fallback、wrapper Auto policy、committed-force 语义和性能门禁完整。
- B1 完成，进入 B2-T1 active pair/mask dataflow；先清理 B1 Task worktree、分支、handoff 与临时 benchmark/review 产物。
- 从集成 HEAD `3d254b6` 创建 `.worktrees/b2-t1` / `task/cuda-opt-b2-t1`；worktree 已由 `.gitignore` 覆盖且当前干净。
- B2-T1 CUDA Release 已配置和构建；`test_gpu_engine_layout`、`test_gpu_batch`、`test_gpu_sim_batch`、`test_gpu_graph_pipeline` 基线 4/4 通过，仅有既有 NVCC/Eigen/Boost warning。
- B2-T1 源码预检确认：`GpuEngine::execute_physical_pipeline()` 每步在 host 以 `B*S*F` 重建 `step_workspace_.active_pairs` 并完整 H2D；device control 已在步尾更新同一 `PairActive` buffer，下一步却被 host 无条件覆盖。
- 用户确认后续 subagent 固定 `gpt-5.6-sol` / High / No Fast；执行计划、根台账和 SDD ledger 已一致，无需修改模型约束。
- 创建 `.superpowers/sdd/task-B2-T1-brief.md`，锁定 TDD、允许写集、mask 语义风险、active-ratio benchmark 和 report contract。
- 派发 B2-T1 implementer `Carson`（agent `019fa7af-0d0b-7810-84e8-44142579a513`），显式 `gpt-5.6-sol` / High，未设置 Fast/service-tier；运行期间由该 agent 独占 GPU。
- B2-T1 implementer 先仅落盘 `test_gpu_graph_pipeline.cpp` RED 草稿并重建 focused target，随后加入允许写集内的 mask-to-pair kernel/engine staging 候选和 stable/low/high-frequency 测试。
- 其后多个 wait 窗口、两次 interrupt 均无回执/report/进程活动，agent 始终 running；已关闭并触发 controller fallback，保留测试先行现场，不切换模型或 Fast。
- Controller 核验 canonical CPU fallback 的 pair 契约是 `active batch && trigger_mask && mutual_stage_mask`；旧 CUDA host staging 缺少 trigger，agent 的测试方向有效。
- 在临时 detached base worktree 仅应用测试 diff 后复现 RED：目标用例 1 case / 6 assertions 中 2 项按预期失败，旧路径未触发 stage 的 `m1=2.97663e-07`、`dm1=3.12547e-06` 而期望 0；生产候选未参与该运行。
- B2-T1 GREEN 候选采用融合 device-derived staging：separation kernel 同时派生 `active && trigger && mutual` pair activity；删除 host `B*S*F` 重建、完整 PairActive H2D 和 device-control 步尾重复 pair 写。
- 完整 CUDA Release build 116/116 成功；focused preset 实际选中 2/2，通过后直接 build-dir 补跑要求的四套件 4/4 通过；cuda-integration 8/8 通过。
- focused compute-sanitizer memcheck：`test_gpu_graph_pipeline` 9/9 cases、49/49 assertions，0 errors；`git diff --check` 通过。
- 固定 workload base/after 各 3 轮、每 artifact 165 GPU rows，schema 全通过；small Direct/Graph 典型 median 分别改善约 6.8%/5.8%，large Direct 约 2.5%，B128 Direct 基本持平。
- 有效 active matrix base/after 各 180 行，覆盖四档 B、五档 active ratio、stable/low/high、20 warm-up/20 measured/3 repeats；全部 mapping/finite/GPU execution 通过且 0 fallback，最差 group median 回退 `-0.7651%`。
- final helper 首次因 `-x c++` 参数污染 archive 产生巨量诊断，已终止并改为 compile/link 两阶段；最终静态库重链接 smoke 180/180 mapping/finite/GPU execution、0 fallback。
- 创建 `.superpowers/sdd/task-B2-T1-report.md`，等待创建唯一 final Task commit 和独立 Sol High review。
- B2-T1 创建唯一 final Task commit `47cee51`；下一步生成固定 review package 并派发 `gpt-5.6-sol` / High / No Fast 独立 reviewer。
- 首次 review-package 因手工补全错误长 SHA 被 Git 拒绝；已用 `git rev-parse` 校正为 `47cee513096aad0fa80c02f3c023b7994699a800`，代码与 commit 未受影响。
- 生成固定 package `.superpowers/sdd/task-B2-T1-review-3d254b6-47cee51.md`（1 commit，27,212 bytes）；派发 reviewer `Raman`（agent `019fa7e7-e9c3-7a02-99c6-2d1b0186b564`），显式 `gpt-5.6-sol` / High / No Fast。
- B2-T1 reviewer 初始 180 秒及 interrupt 后 60 秒均无 verdict，关闭时仍 running；未切模型/Fast，controller 对固定 package 完成 spec/quality + Brooks review。
- B2-T1 review：Spec Compliant、Task Quality Approved、0 Critical/Important、1 Minor；Minor 为 `gpu_engine.hpp` 中仅构造期分配的死 `active_pairs` workspace，因不产生每步成本且 header 不在本 Task 写集，转入 B2-T3/final ledger。
- 将 B2-T1 cherry-pick 到集成分支为 `21c5332`；集成完整 CUDA Release 增量 build 37/37、focused 4/4、cuda-integration 8/8 全部通过。
- B2-T1 完成；下一 Task 为 B2-T2 inactive batch row 与 active solver mapping。Task worktree/branch 将删除，B2 raw benchmark 和 handoff 保留到 B2 Gate 统一清理。
- 用户变更后续 subagent 配置为 `gpt-5.6-luna` / High / No Fast；正式执行计划和 SDD 当前配置已同步，B0-B2-T1 的历史 Sol 记录不改写。
- Task worktree 已删除；普通 `git branch -d` 因 cherry-pick ancestry 保护拒绝后，经 tree equality 验证，按用户清理要求删除 `task/cuda-opt-b2-t1`。
- B2-T2 已从集成 HEAD `96ff542` 创建 `.worktrees/b2-t2` / `task/cuda-opt-b2-t2`，并创建 task brief；worktree 当前未修改生产代码。
- B2-T2 worktree 初次缺少 `build/cuda-release` 配置，串行执行 `cmake --preset cuda-release` 后完整 CUDA Release build `150/150` 成功；仅有既有 NVCC/Eigen/Boost warnings。
- B2-T2 baseline focused tests 直接在 `build/cuda-release` 运行 `test_gpu_batch|test_gpu_solver|test_gpu_assembly`，3/3 通过（总计 6.37 秒）。
- B2-T2 TDD RED：旧 production 下 inactive-row timing gate 失败（`inactive_ms=1.89002`、`active_ms=1.49537`），证明完整 B 行路径未被跳过。
- B2-T2 GREEN：`DeviceMatrixView` 增加 device active mask/host active count；非 Graph 路径做 active-row compaction、cuBLAS active solve、stable-ID scatter；全 inactive 跳过 cuBLAS；Graph 保留 fixed-shape fallback。
- B2-T2 GREEN 验证：19/19 cases、24,775/24,775 assertions；focused `test_gpu_batch|test_gpu_solver|test_gpu_assembly` 3/3；CUDA Release build 无新增工作；compute-sanitizer memcheck `0 errors`。
- B2-T2 controller spec/quality review clean，无 Critical/Important/Minor；Task commit `e123e9f`，集成 commit `9501faa`。方案选择为 non-Graph active compaction，grouping 进入后续 planner backlog。
- B2-T2 Task worktree、分支、临时 helper、base worktree 和 exploratory CSV 已清理；正式 benchmark 不在本 Task 执行，严格延后到全部开发 Task 完成后。
- B2-T2 完成，进入 B2-T3：completion、snapshot、thermal workspace 生命周期；B2 Gate 暂不通过，等待 B2-T3。
- B2-T3 TDD RED：旧 production 在新增 snapshot-count 测试下编译失败，缺少 `ExecutionReport::gpu_thermal_snapshot_count` 契约。
- B2-T3 GREEN：只在 resolved GPU thermal 路径提交 `PreStepCurrents` D2D snapshot；CPU thermal/thermal off 跳过；direct fallback 删除 mutual 与 assembly 间冗余同步；GPU thermal workspace reset/reuse 保持 allocation 稳定。
- B2-T3 GREEN 验证：CUDA Release 98/98；focused paths/thermal/thermal_cpu/resident 4/4；GPU/CPU single/multi 2/2；cuda-integration 8/8；新 paths 11 cases/182 assertions；memcheck 0 errors。
- B2-T3 controller spec/quality review clean，无 Critical/Important/Minor；Task commit `f50d283`，集成 commit `e4c0f8f`；API.md/API_cn.md 已同步 snapshot 诊断字段。
- B2-T3 worktree/分支已清理；B2-T1 raw benchmark/helper artifacts 已清理。B2 Gate 的开发正确性条件完成，正式 benchmark 延后到 B5。
- B2 完成，进入 B3-T1：固定 9 点 mutual 与几何不变量；所有开发 Task 完成前不运行 benchmark。
- B3-T1 已在 `.worktrees/b3-t1` 完成 fixed-9 mutual kernel 实现，Task commit 为 `5e3bcaa`；CUDA 定向 build、filament/coil-pair/precision 3/3、compute-sanitizer 3/3 且 0 errors，通过 `git diff --check`，资源报告无 local/spill。
- B3-T1 正式 benchmark 按用户要求 deferred；已生成 `task-B3-T1-report.md` 和固定 review package，等待 Luna/High/No Fast task reviewer。
- 已预创建 B3-T2 brief；在 B3-T1 review clean 前不派发实现、不修改 B3-T2 生产写集。
- B3-T1 Luna reviewer 三个等待窗口无 verdict，已关闭；Controller 固定 package review clean（Spec/Quality Approved，0 Critical/Important/Minor），Task commit `5e3bcaa` 可集成。
- B3-T1 已 cherry-pick 到集成分支为 `7af551b`；集成前保留的 docs/plan、全局 findings/progress/task_plan 用户工作区改动未被覆盖，下一步为 B3-T2。
- B3-T2 已在 `.worktrees/b3-t2` 完成 `S==1` force reduction 编译期特化；Task commit `8163838`，controller fixed-package review clean、Brooks 100/100，focused 4/4、扩展回归 9/9、四个 memcheck 0 errors；正式 benchmark 按用户要求 deferred。
- B3-T2 已 cherry-pick 到集成分支为 `7f0f2de`；集成 CUDA 增量 build 22/22、`cuda-quick` 30/30 通过，下一步 B3-T3；待清理该 Task worktree/分支。
- B3-T3 已创建 `.worktrees/b3-t3` / `task/cuda-opt-b3-t3`，候选为 D<=32 保留 serial residual，D>32 使用 block-row residual reduction，并新增 D=129 characterization test。
- B3-T3 首次扩展 CTest 返回 5 个 `Not Run`，原因是 worktree 尚未构建其它匹配测试目标；已记录并改为先完整构建匹配 targets，再重跑测试。
- B3-T3 solver/resident memcheck 均为 0 errors，solver racecheck 为 0 hazards；全量 initcheck 的 sanitizer 运行因既有 inactive-row timing assertion 波动失败 1 项（21.6631 ms vs 20.9104 ms），但 initcheck `ERROR SUMMARY` 为 0，后续改跑新增 D=129 case 做内存初始化隔离验证。
- B3-T3 已 cherry-pick 到集成分支为 `f1c128e`；集成 CUDA 增量 build 23/23、`cuda-quick` 30/30、`cuda-integration` 8/8 通过，B3 开发子门完成；正式 benchmark 和性能门禁延后 B5，下一步 B4-T1。

## B5 最终收尾（2026-08-19）

- B4-T1/T2 已完成并集成为 `420064f`、`cff073c`；所有生产开发 Task 在 benchmark 前完成。
- 同环境 baseline worktree 在 `9e50677` 构建 CUDA Release benchmark；首次 shell 包装错误和 `/tmp` 执行尝试均未产生有效 benchmark 证据，随后改用单次显式 `env` 调用完成 3 份有效 baseline artifact。
- baseline/after 各 3 份 artifact 均通过 `check_gpu_benchmark_schema`，每份 165 GPU rows；after 统计实际 GPU 324、fallback 72、setup 99，实际 GPU rows 全部 finite。
- 最终 steady-state 结果：small Direct -2.37%、small Graph -1.25%、medium Direct -7.88%、medium Graph -4.71%、large Direct -2.70%、large Graph -0.49%、batch 128 -0.06%、GPU thermal -0.46%；统一 5% 主线门禁未通过，但无超过 2% 的关键 workload 未解释回退。
- 已创建中文效果报告 `docs/benchmarks/2026-07-28-CUDA-Execution-Optimization-Effectiveness.md`，包含环境、median/p95、fallback 排除、设计目标处置、全仓现代化决策和后续 backlog。
- Luna whole-branch reviewer 连续等待无 verdict，已关闭；controller 对 `3ddd86e..0adf191` 固定 package 完成 fallback review，0 Critical/Important/Minor。
- 已更新执行计划、`task_plan.md`、本文件和 SDD progress；下一步为最终全量命令核验、精确清理和完成状态检查。

## 2026-08-19 回归修复恢复

- 对照实际 Git/worktree 状态后，撤销“B5 已完成”的台账结论：生产分支仍有后续发现的 CUDA 异步 stream 正确性回归，历史 benchmark 不能作为修复后的最终证据。
- 已确认 `execute_physical_pipeline()` 的 execution kernel 使用 non-blocking `context_->stream()`，而 boundary/derivative H2D 使用默认 stream 同步 `cudaMemcpy`；该边界缺少 happens-before。
- 当前隔离诊断现场为 `/tmp/coligunCalc-regression`，未提交且包含临时同步、调试输出和测试诊断。下一步先在干净 `0adf191` 上重现 RED，再实施同 stream async H2D 修复。
- 干净生产代码基线已取得 RED：`test_gpu_vs_cpu_multi` 失败，GPU 首步电流为 0、CPU 为约 139.499；`CUDA_LAUNCH_BLOCKING=1` 对照通过。TDD RED 已成立，下一步在回归 worktree 添加正式稳定性断言并实施同 stream H2D 修复。
- regression worktree 已完成 GREEN 候选：physical pipeline/reset/assembly/init/solver H2D 统一使用 execution stream；控制时间使用持久 workspace，避免异步拷贝引用局部变量。修复后目标测试连续 5/5 通过，下一步是完整 CUDA build、quick/parallel/slow/integration 和 sanitizer。
- CUDA Review R01 clean：检查 execution stream 归属、H2D ordering、初始化局部 vector、reset/test 生命周期和 solver pointer table，未发现新问题。
- CUDA Review R02 clean：复核 `GpuSolver` enqueue-only/validation barrier、cuBLAS/cuSOLVER stream 绑定、device pointer table 更新、active-row compaction、Graph capture 兼容性和 context/solver 释放顺序，未发现新的正确性、生命周期或可执行性能问题。
- CUDA Review R03-pre 发现并修复 state-kernel 边界：`force_reduction_kernel` 的 shared 数组为 512 元素，而旧 `valid_threads()` 放行 1024 threads。先加入 `test_gpu_paths` RED 用例（旧实现返回 `cudaSuccess`），再将上限收紧为 512；修复后定向 CUDA quick `test_gpu_paths` 通过。因出现真实 finding，clean streak 重新计数。
- CUDA Review R03-post clean：复核修复后的 reduction shared-memory 边界、launch contract 和 state-kernel 回归，0 findings；当前修复后 clean streak 为 1/8。
- CUDA Review R04-pre 发现并修复 Graph capture 异常生命周期问题：capture body 抛异常时旧实现遗留 stream capture，导致后续同步返回 `cudaErrorStreamCaptureInvalidated`。先加入 CUDA stream 状态 RED 用例，再让 `GpuGraphCache::capture_and_select()` 捕获异常、结束 capture、销毁临时 graph 并锁定 fallback；修复后 `test_gpu_graph` 通过。clean streak 再次归零。
- CUDA Review R04-post clean：复核 Graph capture/replay、EndCapture error cleanup、runtime mask reuse、fallback lock 和 planner/report 语义，0 findings；当前修复后 clean streak 为 1/8。
- CUDA Review R05 clean：复核 engine/solver/thermal/adaptor 的 device/host workspace ownership、reset/reuse、异常回滚、初始化异步源 buffer 生命周期和析构顺序；在当前单 device contract 下未发现新的可执行正确性或性能问题，clean streak 2/8。
- CUDA Review R06-pre 发现 steady-state launch validation 性能问题：`GpuEngine` 每个 Direct/Graph physical step 反复调用 `cudaPointerGetAttributes()` 和 `cudaGetDeviceProperties()`，固定生命周期的 device buffer 被重复检查，增加 host runtime 开销；该问题与既有 `PerformanceReview(_cn).md` 的 CUDA-07 记录一致。
- R06 修复采用 source-local trusted launch 入口：公共 `launch_*` checked API 保持原有 pointer/device-limit 检查，`GpuEngine` 仅对初始化后不变的 owned buffers 使用 trusted 入口；checked/Graph/Direct/fallback 语义未改变。新增 `src/cuda/gpu_kernel_launch_detail.hpp`，并在 state/mutual launch 实现中保留纯整数契约检查。
- R06 修复定向验证：CUDA Release 构建及 `test_gpu_assembly`、`test_gpu_paths`、`test_gpu_graph`、`test_gpu_vs_cpu_multi`、`test_gpu_sim_batch`、`test_gpu_solver`、`test_gpu_resident_pipeline`、`test_gpu_graph_pipeline` 全部通过；R06-post 尚待完成，clean streak 仍从 0 重新计数。
- CUDA Review R06-post clean：trusted launch 入口仅由 `GpuEngine` 调用，固定 device buffer/stream 生命周期成立；公共 checked API、Graph capture、Direct、Eigen fallback 和 launch shape 整数检查保持；资源报告仍无 local/spill。修复后 clean streak `1/8`。
- CUDA Review R07 clean：复核 device control 的 stage trigger/completion/active-mask 终止判定、compact status 的 finite/residual contract、kernel stream 顺序和 host status barrier；未发现数据竞争、错误终止或可在无 workload 证据下安全改写的性能问题。clean streak `2/8`。
- CUDA Review R08 clean：复核 assembly row-major/RHS 映射、active-row compaction、cuBLAS pointer table、in-place factorization、solution scatter 和 residual barrier；现有 D2D staging 为保留原始 matrix/RHS residual 契约所需，未发现当前可安全合入的新问题。D2D workspace 融合保留为需独立设计和 benchmark 的后续候选。clean streak `3/8`。
- CUDA Review R09 clean：复核 trusted launch 的 source-local 调用边界、公共 checked API 保留的指针/整数/线程/尺寸契约、Direct/Graph 共用 launch 语义和 kernel 资源限制；未发现新的正确性、生命周期或可安全合入的性能问题。CUDA Release 定向构建及 `test_gpu_assembly`、`test_gpu_paths`、`test_gpu_graph`、`test_gpu_vs_cpu_multi`、`test_gpu_sim_batch`、`test_gpu_solver`、`test_gpu_resident_pipeline`、`test_gpu_graph_pipeline` 共 8/8 通过，clean streak 为 `4/8`。
- CUDA Review R10 clean：复核 device solver 的 active-row compaction、cuBLAS pointer table、cuBLAS/cuSOLVER execution-stream 绑定、in-place factorization、residual/status validation barrier 和 Graph capture 分支；未发现新的正确性、生命周期或可安全合入的性能问题。host status 初始化和 `cudaStreamIsCapturing` 保留，避免在缺乏独立证据时改变状态契约。`test_gpu_solver`、`test_gpu_resident_pipeline`、`test_gpu_graph_pipeline` 共 3/3 通过，clean streak 为 `5/8`。
- CUDA Review R11-pre 发现 Graph steady-state host 开销：`execute_physical_pipeline()` 每步把 `stage_mask_`/`mutual_stage_mask_` 复制进 `GpuGraphBoundaryState::runtime_masks`，但 cache selection 只读取 topology，mask 已单独在 execution stream 上传；已改为直接调用 topology overload，消除无消费者的 `O(B*S)` 临时复制。R11-pre 使 clean counter 重置。
- CUDA Review R11-post clean：Graph topology-only selection、runtime mask replay、capture/replay、异常 fallback 和 cache cleanup 复核无新问题。CUDA Release 构建及 `test_gpu_graph`、`test_gpu_graph_pipeline`、`test_gpu_vs_cpu_multi`、`test_gpu_sim_batch` 共 4/4 通过；修复后 clean streak 为 `1/8`。
- CUDA Review R12 clean：复核 Full/Standard/Aggressive mutual 数值语义、固定 9 点索引、cutoff/singular clamp、AGM、fused M/dM、shared reduction 和 kernel 资源；未发现新的正确性、生命周期或可安全合入的性能问题。`cuobjdump --dump-resource-usage` 未发现 mutual/state kernel local memory 或 spill；CUDA Release 的 `test_gpu_engine_physics`、`test_gpu_elliptic`、`test_gpu_filament`、`test_gpu_coil_pair`、`test_gpu_precision` 共 5/5 通过，clean streak 为 `2/8`。
- CUDA Review R13-pre 发现测试输入跨 stream 准备问题：`test_gpu_solver` 使用 legacy default stream 上传 borrowed matrix/RHS/mask，再交给 non-blocking solver execution stream，缺少 happens-before，造成偶发 residual/solution 失败。已将测试 H2D 改为 `context.stream()`，并在 API.md/API_cn.md 明确 borrowed device view 的 stream 与生命周期约束；未向生产 solver 引入全局同步。R13-pre 重置 clean counter。
- R13-post clean：CUDA Release 增量构建成功，`cuda-quick` 30/30、`cuda-integration` 8/8、`test_gpu_solver` 连续 8 次通过；修复后的 clean streak 为 `1/8`。
- CUDA Review R14 clean：复核 physical pipeline、reset、device assembly test、runtime initialization、solver borrowed views 的 H2D/D2D/D2H stream 归属，以及异步拷贝源缓冲区生命周期和 context/BLAS/cuSOLVER 析构顺序；未发现新的正确性、生命周期或可安全合入的性能问题。针对性 CUDA 回归 5/5 通过，clean streak 为 `2/8`。
- CUDA Review R15 clean：复核 GPU thermal workspace 的 device-resident state、thermal kernel stream、D2H observation、inactive-state restore、CPU/GPU thermal 分支、reset/reuse 和资源释放顺序；未发现新的正确性、生命周期或可安全合入的性能问题。`test_gpu_vs_cpu_single` 与 `test_gpu_vs_cpu_multi` 2/2 通过，clean streak 为 `3/8`。
- CUDA Review R16 clean：复核 Graph cache 的 key/variant 生命周期、unordered-map 选择、capture Begin/Body/End/Instantiate 清理、runtime mask replay、replay 失败 fallback lock 和 reset/shutdown 边界；未发现新的正确性、生命周期或可安全合入的性能问题。`test_gpu_graph` 与 `test_gpu_graph_pipeline` 2/2 通过，clean streak 为 `4/8`。
- CUDA Review R17 clean：复核 mutual/separation、assembly、state/control/status kernel 的索引 guard、shape/乘法溢出、线程块与 shared memory、checked/trusted launch 共用语义；未发现新的正确性、生命周期或可安全合入的性能问题。kernel 回归 6/6 通过，`test_gpu_paths` 与 `test_gpu_assembly` memcheck 均为 0 errors，clean streak 为 `5/8`。
- CUDA Review R18-pre 发现 active-row residual 缺口：compaction 跳过 inactive factorization，但 residual kernel 仍对全部 B 行执行 `D*D` 计算并校验；inactive 行非零 RHS 会被误报 residual failure。新增 TDD RED 用例复现后，将 active mask 传入 residual kernel，使 inactive residual 直接为零并跳过计算；同步更新 API.md/API_cn.md。
- CUDA Review R18-post clean：solver active compaction/scatter、mask-aware residual、Graph capture fixed-shape 分支、resident pipeline 与既有 solver 契约均通过；focused solver/resident/Graph 3/3，test_gpu_solver memcheck 0 errors（21 cases、24,789 assertions），修复后 clean streak 为 `1/8`。
- CUDA Review R19 clean：复核 device-control 与 SimBatch/GPU wrapper 的 trigger/completion/active 终止、stable batch ID、host state barrier、excitation 推进和 complete-stage mask 更新；未发现新的正确性、生命周期或可安全合入的性能问题。`test_gpu_vs_cpu_multi` 与 `test_gpu_sim_batch` 2/2 通过，clean streak 为 `2/8`。
- CUDA Review R20 clean：复核 execution policy、backend selection reason、Graph capability fallback、trusted launch 的 checked/trusted 边界，以及 resource-contract 测试覆盖；未发现新的正确性、生命周期或可安全合入的性能问题。当前工作树 CUDA Release 增量构建成功，`test_gpu_execution_policy`、`test_gpu_paths`、`test_gpu_resource_contracts` 3/3 通过，clean streak 为 `3/8`。
- CUDA Review R21 clean：复核 Graph topology key 与 runtime mask 分离、capture/replay/fallback lock、resident pipeline allocation reuse/reset 和 policy/report 交互；未发现新的正确性、生命周期或可安全合入的性能问题。当前工作树 `test_gpu_execution_policy`、`test_gpu_graph`、`test_gpu_resource_contracts`、`test_gpu_resident_pipeline` 4/4 通过，clean streak 为 `4/8`。
- CUDA Review R22 clean：复核 execution stream 上的 physical pipeline、thermal copy、reset/init H2D、D2D snapshot、D2H barrier、异步 host 源生命周期和 step 热路径同步；未发现新的正确性、生命周期或可安全合入的性能问题。`test_gpu_vs_cpu_single`、`test_gpu_thermal`、`test_gpu_resident_pipeline` 3/3 通过，resident `compute-sanitizer memcheck` 为 0 errors，clean streak 为 `5/8`。
- CUDA Review R23 clean：复核 mutual/state/assembly/control/status kernel 的索引 guard、尺寸乘法溢出、线程块/shared-memory 边界、Full/Standard/Aggressive 数值路径和 checked/trusted launch 语义；未发现新的正确性、资源或可安全合入的性能问题。CUDA Release 定向测试 5/5、precision 直接测试 5/5 cases、paths/assembly memcheck 均为 0 errors，资源报告无 local/stack/spill，clean streak 为 `6/8`。
- CUDA Review R24 clean：复核 device solver 的 active-row compaction、pointer table 更新、cuBLAS execution stream、solution scatter、mask-aware residual、status barrier，以及 Graph fixed-shape 分支；未发现新的正确性、生命周期或可安全合入的性能问题。solver/Graph pipeline/batch 回归通过，solver memcheck 为 0 errors，Graph pipeline racecheck 为 0 hazards，clean streak 为 `7/8`。
